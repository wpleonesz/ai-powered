const SEARCHABLE = {
  id: true,
  name: true,
  description: true,
  createdAt: true,
  updatedAt: true,
};

const UPDATABLE = {
  name: true,
  description: true,
};

const CREATABLE = {
  name: true,
  description: true,
};

export default {
  SEARCHABLE,
  UPDATABLE,
  CREATABLE,
};
