const SEARCHABLE = {
  id: true,
  query: true,
  result: true,
  createdAt: true,
  updatedAt: true,
};

const UPDATABLE = {
  query: true,
  result: true,
};

const CREATABLE = {
  query: true,
  result: true,
};

export default {
  SEARCHABLE,
  UPDATABLE,
  CREATABLE,
};
