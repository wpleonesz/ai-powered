const DEFAULT = {
  id: true,
  code: true,
  name: true,
  subname: true,
  description: true,
  icon: true,
  installed: true,
  active: true,
};

const schemas = { DEFAULT };

export default schemas;
