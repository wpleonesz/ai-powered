const DEFAULT = {
  active: true,
  Role: {
    select: {
      id: true,
      name: true,
    },
  },
  Menu: {
    select: {
      id: true,
      name: true,
    },
  },
};

const schemas = { DEFAULT };

export default schemas;
