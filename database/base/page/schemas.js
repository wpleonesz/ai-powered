const DEFAULT = {
  id: true,
  name: true,
  url: true,
  active: true,
  moduleId: true,
  Module: {
    select: {
      name: true,
    },
  },
};

const schemas = { DEFAULT };

export default schemas;
