const DEFAULT = {
  id: true,
  name: true,
  description: true,
  sku: true,
  price: true,
  stock: true,
  createdAt: true,
  updatedAt: true,
  active: true,
};

const schemas = { DEFAULT };

export default schemas;
